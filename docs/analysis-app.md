# Analysis app notes (superseded)

This file is kept at its original path to preserve any inbound links. The C# / Avalonia analysis-app design described in earlier revisions is no longer the intended direction for this project.

The host tooling is Python and lives under `tools/`. See:

- [ENTROPY_CAPTURE_PIPELINE.md](ENTROPY_CAPTURE_PIPELINE.md), section 9, for the host reader design.
- `tools/README.md` for setup and run commands.
- `tools/src/tinychaos/` for the package source.

The C# plan is dropped. Reasons captured in `ENTROPY_CAPTURE_PIPELINE.md` section 1.
